@page "/returnorder/purchaseproductforreturning"
@inject 

<h3>PurchaseProductForReturning</h3>
<div class="container-fluid">
    <div class="row" hidden="@hideErrors">
        <p>here i will show the errors</p>
    </div>
    <div class="row">
        <div class="col-8">
            <div class="row">
                <p>here i will show the filters</p>
            </div>
            <div class="row">
                <p>here i will show the list of movies</p>
            </div>
        </div>
        <div class="col-2">
            <p>here i will show the renting cart</p>
        </div>

    </div>

</div>

    @code {
    private ICollection<PurchaseProductForReturnDTO> purchaseProductForReturn;
    private bool hideErrors { get { return errors.Length == 0; } }
    private string errors = "";
    private string userName { get; set; }
    private string productName { get; set; }
    private string quantity { get; set; }


    public async void SearchPurchaseProductForReturning()
    {
        try
        {
            movies = await apiclient.GetPurchaseProductsForReturning(userName, productName, quantity);

        }
        catch (ApiException ex)
        {
            logger.LogError($"{DateTime.Now} Error: {ex}");
            errors = "Error while connecting to the system";
        }
        StateHasChanged();



}
